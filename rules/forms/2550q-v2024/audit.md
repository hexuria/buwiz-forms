# Audit â€” 2550Q April 2024 ENCS

- Revision is bound independently by runtime identity, printed header, installed help, official form PDF, and official guidelines.
- The shared `string-util.js`, loaded `eBIRTools.vbs`, and external encryption/transport helper identities reached by the pinned HTA are separately hashed. Helper identity is not treated as proof of an internal algorithm or transport verification.
- Inventory is the union of 160 editable-save pseudo-div keys and 159 encrypted-final-copy pseudo-div keys. `dateFiled` is the sole editable-only pseudo-div key; Final Copy emits it separately as standalone metadata.
- Runtime source contains 219 live static controls and 28 unbounded indexed families.
- The value-free serialization binding inventory covers all 160 observed occurrences, partitions the 28 indexed families into seven unbounded `assigned-stable-id` groups, and records artifact-specific codecs. It deliberately exposes no values. All 159 live serialized-control occurrences have candidate identities: 119 static controls plus 40 materialized repeated-family occurrences. The executable surface contains 66 singleton fields plus 28 repeated-family descriptors; 44 derived/alias and nine workflow/credential/UI-state controls remain identity-only documented bindings. Generated `dateFiled` projects from the required immutable `local-current-date` context snapshot, closing all 160 observed value-source identities; its production clock/timezone provider and stable-instance relation to official live DOM/display order remain unresolved.
- All 66 executable candidate singleton identities have closed core/GPUI raw bindings. Amended-return Yes/No and short-period Yes/No use four independent raw-backed choices with exact group capture and import restoration. The ten profile-backed identity/contact controls remain absent rather than being synthesized from typed profile data, and preserve exact reviewed-import or explicitly edited text when materialized.
- Main Validate, Save preflight, schedule validators, additional-row validators, calculations, and serialization paths were inspected.
- First-error order and exact alerts are recorded.
- Dummy-only negative cases bind to rule IDs; taxpayer values and email-bearing filenames are excluded.
- Confirmed defects include the stale future-period exception, nonblank-only TIN/email checks, unreachable Item 19 branch, wrong Save alert noun, Schedule 1 zero-life mismatch, Schedule 3 year-boundary contradiction, suspicious multi-row references, and the Item 15/61 feedback assignment.
- Source-static Final Copy/Submit defects are separately pinned: `checkNetConnection()` makes the no-connection local-copy branch unreachable; failed-submit retry calls undefined `emailResend()` instead of loaded `reSendEmail()`; the encryption helper exit code is ignored; and a failed `saveXML(true)` can leave `sendEmail()` dereferencing `undefined` before its transport `try` block.
- The v2 candidate records the reachable Final Copy-to-enrollment and enrollment-to-Submit landmarks only as `documented_only`. It inventories separate package-specific editable-save, finalized-save, and encrypted-final-copy artifacts as `documented_only` with no nodes; materialization and every online side effect remain unavailable.
- The settled v2 candidate source-set identity is `e51819028a4b2199debac87ca024fbe05b3ccb518cccbe76a436e6f8e08d45e0`; its normalized audit digest is `67acef674a481ec110c562a6ad72896500a433c74c986557f304c35af4b3ee54`, serialization-contract digest is `c623ab7883fed79c150c5b574ab7c6682ce2e5c79cb76ad3a3df3ee816196cae`, and generated-output digest is `d1fd97c502377475c7ba4f12fdb7a02021ab48ad56ed629cefb7b2594f279e71`.
- The strict generated-file check passes at the pinned output digest, and the clean combined `bir-rules-codegen` plus `bir-rules` command passes 204 tests across five suites. The reviewed registry remains empty.
- Final strict repository audit passed with `-RequireJsonSchema`: 43 forms, 520 legacy JSON files, 659 JSON files total (139 v2), 9,592 fields, 2,007 validations, 623 calculations, 1,354 negative fixtures, and 216 schema documents. Structural audit and JSON Schema validation both reported `pass`; stderr was empty.
