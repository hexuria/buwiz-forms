# 1702Q v2018C fixtures

All data in these fixtures is synthetic. `positive-minimal.json` describes a local-validation baseline and does not authorize saving or submission. `negative-cases.json` preserves exact official rejection text and bug-compatible silent paths. `calculation-boundaries.json` records formula boundaries and known official defects. `runtime-control-inventory-v796.json` is the exact DOM/serialization inventory generated from the pinned HTA.

No fixture was submitted online. The existing representative save was read only.
