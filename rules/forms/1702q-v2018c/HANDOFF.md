# Handoff

`1702q-v2018c` is complete as a research package for priority item 3.

Consumers should key the revision by `1702Qv2018C`, retain occurrence-aware handling for the duplicate `txtTelNum`, implement Validate and Save as distinct ordered rule sets, and preserve official-bug-compatible outcomes only when exact runtime parity is required. Recommended application behavior is recorded beside each defect.

Before using these rules in product code, add independent policy review for ATC/rate correctness and decide explicitly whether official defects are reproduced, warned on, or corrected. Do not infer filing success from `validated`; the workflow distinguishes local validation, final-copy confirmation, offline-copy fallback, submission pending, submitted, and retry.

Next queue item: priority 4, `1601FQ`.
