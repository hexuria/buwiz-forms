# Handoff

`1603q-v2018` is complete as priority item 5. Consumers must not “fix” the official behavior silently: retain the source-compatible result when parity is required, while exposing the recommended corrected Schedule 1 total and `getFullYear` logic as explicit policy decisions.

Next priority item: `1600VT`.
