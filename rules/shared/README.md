# Shared official behavior

These artifacts describe cross-form behavior only where an inspected form invokes the shared implementation. A shared rule is not assumed applicable to every form merely because it exists in the package.

The initial shared inventory is anchored to Offline eBIRForms 7.9.6.0 assets observed on 2026-07-22. Each form must cite the shared rule it actually invokes.
