# Form 2200M — Excise Tax Return for Mineral Products

> **Tier B** | Filing Period: Monthly | Type ID: `2200Mv2018`

## Goal

Generate a complete typed implementation for BIR Form 2200M and promote it to `ImplementedInApp`.

## Status

- [ ] XML savefile acquired
- [ ] `parse_payload.py` executed
- [ ] eFPS manifest parsed (available)
- [ ] Formtype layout generated (`formtypes/2200Mv2018/`)
- [ ] `form_2200m.rs` generated (Rust struct + recompute + validation)
- [ ] `form_2200m_xml.rs` generated (BIR field mapping)
- [ ] `form_2200m_view.rs` generated (desktop UI)
- [ ] Wired into `forms/mod.rs` (`FormDraft` enum)
- [ ] Added to `FILEABLE_FORM_CODES` in `support_level.rs`
- [ ] Added to `fileable_form_type_id()` → `2200Mv2018`
- [ ] `cargo build && cargo test` passes

## Computation Logic

```
Value/Volume × excise rate → Tax Due → Penalties → Total
```

## Notes

Covers coal, metallic/non-metallic minerals, indigenous petroleum.

---

## Source Materials

### PDF

Directory: `~/Downloads/forms/2200Mv2018/`
File: `2200-M Jan 2018 ENCS v2 final version.pdf`

### eFPS Manifest

File: `docs/efps_manifests/2200M.json`

Use this to identify computed vs user-entered fields:
```bash
python3 .agent/skills/form-generator/scripts/parse_efps_manifest.py \
  docs/efps_manifests/2200M.json \
  .generated/2200m/
```

---

## Step-by-Step Instructions

### Step 1: Generate Savefile (Windows)

Open eBIRForms on Windows → select Form 2200M → fill minimum fields:
- TIN: `000-000-000-000`
- Name: `TEST TAXPAYER`
- RDO: `039`
- Period: current year
- Monetary fields: `1,000.00`
- Click **Save**

Copy the `.xml` file to `~/Downloads/forms/2200Mv2018/`

### Step 2: Discover Sources

```bash
python3 .agent/skills/form-generator/scripts/discover_sources.py \
  ~/Downloads/forms/2200Mv2018/
```

### Step 3: Parse Payload

```bash
python3 .agent/skills/form-generator/scripts/parse_payload.py \
  ~/Downloads/forms/2200Mv2018/<SAVEFILE>.xml \
  .generated/2200m/
```

### Step 4: Generate Formtype Layout

```bash
python3 .scripts/generate_formtype.py \
  --input ~/Downloads/forms/2200Mv2018/2200-M Jan 2018 ENCS v2 final version.pdf \
  --form-id 2200Mv2018 \
  --title "Excise Tax Return for Mineral Products" \
  --detect-fields
```

### Step 5: Generate Typed Form

```bash
python3 .agent/skills/form-generator/scripts/generate_typed_form.py \
  .generated/2200m/analysis.json \
  --target-dir crates/bir-core/src/forms \
  --view-dir crates/bir-desktop/src/views \
  --efps .generated/2200m/efps_fields.json
```

### Step 6: Integration

1. Add to `crates/bir-core/src/forms/mod.rs`:
   ```rust
   pub mod form_2200m;
   pub mod form_2200m_xml;
   ```
   Add `FormDraft::Form2200M(Box<form_2200m::Form2200MDraft>)` variant.

2. Add to `crates/bir-core/src/temporal/support_level.rs`:
   ```rust
   // In FILEABLE_FORM_CODES:
   "2200M"
   // In fileable_form_type_id():
   "2200M" => Some("2200Mv2018"),
   ```

3. Add to `crates/bir-desktop/src/views/mod.rs`:
   ```rust
   pub mod form_2200m_view;
   ```

### Step 7: Validate

```bash
cargo build && cargo test --package bir-core
```
