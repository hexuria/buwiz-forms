# Wave 0 diagnostic review lists

Two sweeps across all ten converted forms, now that each has a same-rasterizer
chromium reference. Both produce **candidates for review, never defect counts**.

## Comb-capacity review list

Generated 2026-07-20 by
`packages/form-renderer/visual/tools/comb-capacity-audit.spec.ts` across all
ten converted forms, now that every form has a same-rasterizer chromium
reference to measure against.

**These are candidates, not defects.** The audit counts our declared cells and
the official form's interior dividers inside the same rectangle. A mismatch is
a question for a reviewer against the pinned PDF. Nothing here may be "fixed"
by changing a declared capacity until the official field has been read.

Severity is assigned so review effort goes where it pays:

- **capacity** — a difference of two or more cells at a plausible pitch
  (>= 20 px). This is the class the one confirmed defect belonged to: 2551Q
  Item 12A declared 26 where the official has 16.
- **off-by-one** — a single-cell difference at a plausible pitch. Usually
  ambiguity about whether the field's own border fell inside the measured
  rectangle. Real, but far more likely a measurement boundary than a defect.
- **low-confidence** — measured pitch is 0 or under 20 px, meaning the scan
  locked onto glyph strokes or borders rather than guides. The count is not
  evidence of anything and the rectangle probably does not correspond to the
  official field at all.

| Form | Fields | capacity | off-by-one | low-confidence |
| --- | ---: | ---: | ---: | ---: |
| `0605:1999` | 21 | 0 | 0 | 18 |
| `0619E:2018` | 47 | 0 | 2 | 0 |
| `0619F:2018` | 63 | 0 | 1 | 1 |
| `1601C:2018` | 106 | 4 | 6 | 1 |
| `1701:2018` | 283 | 2 | 4 | 6 |
| `1701Q:2018` | 116 | 4 | 19 | 4 |
| `1702MX:2018C` | 33 | 6 | 5 | 5 |
| `1702RT:2018C` | 207 | 10 | 17 | 4 |
| `2550Q:2024` | 138 | 3 | 39 | 3 |
| `2551Q:2018` | 96 | 0 | 0 | 0 |
| **total** | | **29** | **93** | **42** |

**2551Q is clean at 0/96, and it is the control**: it is the only form whose
geometry has been worked, and its single capacity defect was found and fixed
first. The other nine have had no capacity review at all, so a non-zero count
is the expected result rather than a surprise.

0605's 18 low-confidence rows are one pattern repeating: its combs are drawn as
ruled boxes rather than tick runs, so the divider scan finds nothing to measure.
That is a detector limitation on an older-generation form, not a finding about
0605.

Per-form JSON with every field, rectangle, measured pitch and tone is
regenerated by running the audit with `COMB_AUDIT_FORM`,
`COMB_AUDIT_REVISION`, `COMB_AUDIT_FIXTURE` and `COMB_AUDIT_REFERENCES` set.

## Wave 0 structural review list

Generated 2026-07-20 by
`packages/form-renderer/visual/tools/structural-defects.spec.ts` across all
ten converted forms. Clusters are capped at the twelve largest per page, so
these are the worst defects, not an exhaustive list.

Each cluster is classified by comparing the official and rendered stroke
thickness at the offset where our structure was actually found:

- **weight** — our stroke is thinner than the official one in place. 2551Q's
  largest defect was this: official part boundaries are 1.5pt and we drew 1pt,
  so 1130 px went unmatched while the rule looked correctly positioned.
- **weight + moved** — thinner *and* displaced.
- **displaced** — same thickness, wrong position.
- **missing or extra** — recovers under 90% at any offset, so no single
  translation explains it. Usually structure we do not draw at all.

| Form | weight | weight+moved | displaced | missing/extra | largest cluster |
| --- | ---: | ---: | ---: | ---: | ---: |
| `0605:1999` | 0 | 10 | 9 | 5 | 4,535 px |
| `0619E:2018` | 0 | 1 | 7 | 4 | 2,321 px |
| `0619F:2018` | 2 | 0 | 4 | 6 | 2,321 px |
| `1601C:2018` | 4 | 3 | 6 | 11 | 2,321 px |
| `1701:2018` | 8 | 7 | 14 | 19 | 3,651 px |
| `1701Q:2018` | 2 | 5 | 13 | 4 | 1,364 px |
| `1702MX:2018C` | 8 | 14 | 12 | 14 | 3,956 px |
| `1702RT:2018C` | 4 | 6 | 14 | 24 | 5,065 px |
| `2550Q:2024` | 0 | 6 | 8 | 10 | 2,745 px |
| `2551Q:2018` | 3 | 1 | 2 | 16 | 134 px |

**2551Q's largest remaining cluster is 134 px. Every other form is between
1,364 and 5,065 px** — ten to thirty-eight times larger. That gap is what
calibration is worth: 2551Q started this session at 1,130 px on page 1.

Weight defects are common rather than a 2551Q peculiarity, which matters
because the fix that worked there is likely to repeat: official part-boundary
strokes are heavier than ordinary interior rules, and a renderer that uses one
border width for both leaves the top row of every boundary unmatched.

**A correction worth recording.** The first sweep reported zero weight defects
on all nine forms. That was a tool artifact, not a finding: the thickness probe
sampled our render at the *official* position, which returns nothing for any
wholly displaced rule, so every weight defect was misfiled as missing
structure. Probing at the matched offset instead surfaced 81 weight-related
clusters. A diagnostic that reports a clean bill of health deserves the same
scepticism as one that reports a problem.

## The transferable finding: Chromium floors border-width to integer CSS px

Established while calibrating 1601C, verified empirically at several device
scale factors rather than inferred, and it changes how every remaining form
should be approached.

Chromium rounds `border-width` **down to whole CSS pixels**, independent of
device scale factor. Measured ladder:

| declared | rendered |
| --- | --- |
| 0.5pt – 1.4pt | 1 px (0.75pt) |
| 1.5pt – 2pt | 2 px (1.5pt) |
| 2.25pt – 2.5pt | 3 px (2.25pt) |
| 3pt | 4 px (3pt) |

Two consequences:

1. **A declared weight hierarchy can be fiction.** All 382 bordered edges in
   1601C render at exactly 1 CSS px despite the stylesheet declaring `.55pt`,
   `.7pt`, `1pt` and `1.1pt`. Four distinct declared weights, one rendered
   weight.
2. **2551Q's fix worked for a different reason than assumed.** Changing part
   boundaries from 1pt to 1.5pt succeeded because it crossed the 1px→2px
   bucket, not because 1.5pt is a BIR weight. The pattern therefore transfers
   as a **mechanism**, not as a constant — and only official weights that land
   on the ladder are expressible at all. 1601C has official strokes measuring
   ~1.10pt and ~1.16pt, which sit between buckets; 1.10pt is closer to today's
   rendered 0.75pt than to 1.5pt, so "correcting" it would make it worse.

Check the rendered width, not the declared one, before treating any weight as a
defect.

### 1601C calibration result

Page 1 Poppler 11.8669% → 11.5612%, chromium 12.3656% → 12.1082%. Page 2
byte-identical. Untoleranced ink measurement (no dilation, so neither side can
hide under the other's tolerance — the blind spot that hid a 2551Q regression)
shows **both** directions improving on **both** references: false ink −1272
(Poppler) / −1358 (chromium), missed ink −5894 / −5980.

Three findings that would have caused damage if acted on naively:

- The three 1486px clusters reported as weight deficits are the opposite:
  **ours is heavier** than the official (0.51pt vs 0.38pt). The tool's
  `official=2px/ours=1px` is a sub-pixel phase artifact of a stroke straddling
  two rows at partial coverage.
- Under `box-sizing: border-box` a bottom border grows **upward**, so
  thickening would have moved ink *away* from the official stroke on three of
  five candidate targets.
- Page 1 has no global registration offset — every one of 120 alternative
  whole-page shifts scored worse — but it does drift progressively, roughly
  +2.3px at y≈315 to −4.4px by y≈1350. Local correction has ~20pp of headroom;
  global registration has none.

Page 2's dominant residual is **text reflow** in the guidelines block, not
structure: the text is 89% present but line-wrapping diverges and the official
continues past where our block ends. No border or registration change addresses
it.

### All ten specs now measure against the chromium reference

The nine forms' parity specs read their Poppler rasters until 2026-07-20; they
now read the same-rasterizer chromium references, matching 2551Q and matching
the structural and capacity diagnostics.

**The effect was smaller and less uniform than expected, and that is worth
recording.** Predicting from 2551Q — whose number rose 6.65% to 7.34% when it
was retargeted — suggested every form would rise by roughly its noise floor.
Measured, the deltas are mixed and small:

| Form | page | Poppler | chromium | delta | pinned floor |
| --- | ---: | ---: | ---: | ---: | ---: |
| `0605` | 1 | 7.4394% | 7.0168% | −0.4226 | 1.55% |
| `0619F` | 1 | 8.3773% | 9.1610% | +0.7837 | 3.84% |
| `1601C` | 1 | 11.5612% | 12.1082% | +0.5470 | 3.76% |
| `1701` | 3 | 12.2539% | 12.1645% | −0.0894 | 3.45% |
| `1702RT` | 4 | 11.3909% | 10.5984% | −0.7925 | 2.37% |
| `2550Q` | 2 | 9.8479% | 10.0734% | +0.2255 | 3.40% |

Nine of twenty-two pages moved *down*. The floor does not simply add: it is the
difference between two renderings of the official page, and our render already
differs from both in ways that partly overlap it. So the earlier claim that
these numbers were "flattered by a floor now excluded" was too strong — the
correction is real but its size is not predictable from the floor.

The value of the change is consistency rather than magnitude: every form is now
measured against the same reference its structural and capacity diagnostics
use, so numbers from different tools can be compared without asking which
rasterizer produced them.
