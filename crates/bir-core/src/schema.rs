//! Form schema engine — JSON-driven form definitions.
//!
//! Each BIR form is defined as a JSON schema with fields, types,
//! validation rules, and layout information. Adding a new form
//! means adding a JSON file, not writing new Rust code.

// TODO: Schema struct definitions (FormSchema, FormField, FieldType)
// TODO: Schema loader (embedded or from file)
// TODO: Schema-to-BirXml mapper
