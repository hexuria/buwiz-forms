# Rust build artifacts
/target/
**/target/

# Cargo lock is committed for applications
# Cargo.lock  # Keep committed — we are an application, not a library

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
*~

# Environment & secrets
.env
.env.local
.env.*.local
*.key
*.pem

# Database files (contain sensitive taxpayer data)
*.db
*.db-journal
*.db-wal
*.db-shm

# User data directory
/data/
/profiles/

# Test artifacts
/test-output/

# Calibration scratch and test artifacts — large, machine-local, never evidence.
# `tmp/` holds built app binaries and release zips; committing either would put
# hundreds of megabytes of non-reproducible output into the corpus history.
/tmp/
/test-results/

# Generated PDF files
*.pdf
*.lock
!/Cargo.lock

# AI Agent Artifacts & Scratchpads
.scratch/
.agents/
.antigravitycli/

# Runtime logs — never commit these
*.log
crash.log
debug.log
dispatch.log
error.log

# CI/dev generated artifacts — use justfile targets, not committed files
clippy_errors.txt
test_errors.txt

# Local interpreter and frontend build output
**/__pycache__/
*.py[cod]
/.ruff_cache/
/apps/form-calibration/dist/

# Machine-local Codex settings
/.claude/settings.local.json

# Validation codegen transaction leftovers
/crates/bir-rules/src/generated.bir-rules-codegen-backup-*/
/crates/bir-rules/src/generated.bir-rules-codegen-staging-*/

# Stray local command-output artifact
/470

# Reverse-engineering workspace artifacts
strings_bir.txt
symbols.txt
objc_classes.txt

# One-off scratch files
scratch*.js
scratch*.rs
fix_formtypes*.js
test_objc.rs
test_windows.rs

# Node JS
node_modules/
/assets/form-renderer/
/assets/form-renderer-build-identity.json
/test-results/form-renderer/

# Stow directory (managed separately)
.stow/

# Stowed & Experimental Ideation Files
.windsurfrules
.agent/

# Internal documentation and exploratory scripts
.docs/
.scripts/
.impl/
.formfields/

.keys/
*.p8
.archives
